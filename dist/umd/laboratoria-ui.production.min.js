(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory(require('@material-ui/core/styles')) :
  typeof define === 'function' && define.amd ? define(['@material-ui/core/styles'], factory) :
  (global['laboratoria-ui'] = factory(global['@material-ui/core/styles']));
}(this, (function (styles) { 'use strict';

  var theme = styles.createMuiTheme({
    typography: {
      fontFamily: ['Open Sans,sans-serif'],
      title: {
        fontFamily: 'Bitter,serif',
        fontWeight: '700',
        lineHeight: '100%'
      },
      subheading: {
        fontFamily: 'Bitter,serif',
        fontWeight: '700',
        lineHeight: '100%'
      },
      button: {
        fontSize: '16px',
        textTransform: 'uppercase',
        fontWeight: 900,
        fontFamily: 'Open Sans,sans-serif',
        color: 'rgba(0, 0, 0, 0.87)'
      }
    },
    palette: {
      primary: {
        main: '#ffe521'
      }
    }
  });

  var index = {
    Theme: theme
  };

  return index;

})));
