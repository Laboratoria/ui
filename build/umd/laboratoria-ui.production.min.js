(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory(require('@material-ui/core/styles'), require('react'), require('@material-ui/core')) :
  typeof define === 'function' && define.amd ? define(['@material-ui/core/styles', 'react', '@material-ui/core'], factory) :
  (global['laboratoria-ui'] = factory(null,global.React,null));
}(this, (function (styles,React,core) { 'use strict';

  React = React && React.hasOwnProperty('default') ? React['default'] : React;

  var theme = styles.createMuiTheme({
    typography: {
      fontFamily: ['Open Sans,sans-serif'],
      title: {
        fontFamily: 'Bitter,serif',
        fontWeight: '700',
        lineHeight: '100%'
      },
      subheading: {
        fontFamily: 'Bitter,serif',
        fontWeight: '700',
        lineHeight: '100%'
      },
      button: {
        fontSize: "16px",
        textTransform: "uppercase",
        fontWeight: 900,
        fontFamily: 'Open Sans,sans-serif',
        color: "rgba(0, 0, 0, 0.87)"
      }
    },
    palette: {
      primary: {
        main: '#ffe521'
      }
    }
  });

  var style = {
    title: {
      marginTop: 10,
      color: 'blur',
      fontSize: 24
    }
  };

  var MainTitle = function MainTitle(props) {
    var classes = props.classes;

    return React.createElement(
      core.Typography,
      { className: classes.title, variant: 'headline', gutterBottom: true },
      props.children
    );
  };

  var MainTitle$1 = styles.withStyles(style)(MainTitle);

  var index = {
    Theme: theme,
    MainTitle: MainTitle$1
  };

  return index;

})));
