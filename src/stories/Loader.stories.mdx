import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { Loader } from '../components';

<Meta title="Components/Loader" component={Loader} />

# Loaders

### How to use

```js
import { Loader } from 'laboratoria-ui';
```

Existen tres medidas estándar para el `Loader`:

- `"small" : 30px`
- `"medium" : 40px` (default)
- `"large" : 50px`

```js
<Loader size="small" />
<Loader size="medium" />
<Loader size="large" />
```

También se puede enviar una medida personalizada utilizando el prop `size`:

```js
<Loader size={80} />
```

El componente `Loader` tiene dos colores:

- `"primary" : #ffe521` (default)
- `"secondary" : #000000`

```js
<Loader color="primary" />
<Loader color="secondary" />
```

El componente `Loader` utiliza por defecto `size="medium"` y `color="primary"`.

## Primary Loader

<Canvas>
  <Story name="Primary" decorators={[(Story) => <div className="loaders-container"><Story/></div>]}>
    <Loader size="large" />
    <Loader />
    <Loader size="small" />
  </Story>
</Canvas>

## Secondary Loader

<Canvas>
  <Story name="Secondary" decorators={[(Story) => <div className="loaders-container"><Story/></div>]}>
    <Loader color="secondary" size="large" />
    <Loader color="secondary" />
    <Loader color="secondary" size="small" />
  </Story>
</Canvas>

### View the source

Aquí puedes revisar el código del componente: [Loader](https://github.com/Laboratoria/ui/blob/master/src/components/Loader/Loader.jsx)