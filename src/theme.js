import { createMuiTheme } from 'createMuiTheme';

const theme = createMuiTheme({
  typography: {
    fontFamily: [
      'Bitter',
    ],
  },
  palette: {
    primary: {
      main: '#ffe521',
    },
  },
})

export default theme;